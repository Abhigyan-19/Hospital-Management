import { useState } from 'react';
import toast from 'react-hot-toast';
import { useAuth } from '../context/AuthContext';
import { Button, Card, Input } from '../components/ui';

export default function Settings() { const { user } = useAuth(); const [form, setForm] = useState({ name: user?.name || '', email: user?.email || '', phone: '+91 98765 43210' }); const [password, setPassword] = useState({ current: '', next: '', confirm: '' }); const saveProfile = (event) => { event.preventDefault(); toast.success('Profile details saved.'); }; const savePassword = (event) => { event.preventDefault(); if (!password.current || password.next.length < 6 || password.next !== password.confirm) return toast.error('Please check your password fields.'); setPassword({ current: '', next: '', confirm: '' }); toast.success('Password updated.'); }; return <div className="mx-auto max-w-4xl space-y-6"><div><p className="font-bold text-teal-700">Your account</p><h2 className="mt-1 text-3xl font-black text-ink sm:text-4xl">Profile & settings</h2><p className="mt-2 text-navy-700">Keep your details and access settings up to date.</p></div><Card><h3 className="text-xl font-extrabold">Personal details</h3><form onSubmit={saveProfile} className="mt-5 grid gap-5 sm:grid-cols-2"><Input label="Full name" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /><Input label="Work email" type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} /><Input label="Phone number" value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} /><div><span className="mb-2 block font-bold">Role</span><div className="min-h-11 rounded-lg border border-navy-100 bg-navy-50 px-4 py-2 font-extrabold text-navy-700">{user?.role || 'STAFF'}</div></div><Button type="submit" className="sm:col-span-2">Save profile</Button></form></Card><Card><h3 className="text-xl font-extrabold">Change password</h3><form onSubmit={savePassword} className="mt-5 space-y-5"><Input label="Current password" type="password" value={password.current} onChange={(event) => setPassword({ ...password, current: event.target.value })} /><div className="grid gap-5 sm:grid-cols-2"><Input label="New password" type="password" value={password.next} onChange={(event) => setPassword({ ...password, next: event.target.value })} /><Input label="Confirm new password" type="password" value={password.confirm} onChange={(event) => setPassword({ ...password, confirm: event.target.value })} /></div><Button type="submit" variant="secondary">Update password</Button></form></Card></div>; }
